import type { CardModule } from "./_types";

// Machine fuel — emitted by the Solar Array, consumed by every powered machine.
export const power: CardModule = {
  defId: "power",
  name: "Power",
  category: "power",
};
